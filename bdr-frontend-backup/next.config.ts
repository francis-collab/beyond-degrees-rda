// next.config.ts
import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // React Compiler is EXPERIMENTAL — DISABLED
  // reactCompiler: true,
};

export default nextConfig;